# Ignore Firebase-related local files
.firebase/
firestore.rules
storage.rules

# Ignore Node.js dependencies (if any)
node_modules/

# Ignore build artifacts (if you add build tools)
dist/
build/

# Ignore GitHub workflow files if they are temporary
.github/workflows/firebase-hosting-merge.yml

# Ignore environment files (if any sensitive keys are used)
.env
.env.local
.env.production
.env.*.local

# macOS-specific files
.DS_Store

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Optional: Exclude VSCode settings if sharing project with others
.vscode/

# Ignore runtime files
*.log
*.tmp

# Ignore deployment cache (if used by tools)
.cache/
